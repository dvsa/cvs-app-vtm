import { Meta, Story } from '@storybook/angular';

import { LandingPageButtonComponent } from './landing-page-button.component';

export default {
  title: 'Landing Page Button',
  component: LandingPageButtonComponent,
} as Meta;

export const Primary: Story = () => ({
  props: {
  },
});
