import { NestingLevelDirective } from './nesting-level.directive';

describe('NestingLevelDirective', () => {
  it('should create an instance', () => {
    const directive = new NestingLevelDirective();
    expect(directive).toBeTruthy();
  });
});
