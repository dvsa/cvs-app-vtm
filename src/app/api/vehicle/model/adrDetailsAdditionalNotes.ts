export interface AdrDetailsAdditionalNotes { 
    /**
     * Optional for all vehicle types
     */
    number?: Array<string>;
    /**
     * Optional for all vehicle types
     */
    guidanceNotes?: Array<string>;
}