export interface AuthIntoService { 
    /**
     * Used only for TRL
     */
    cocIssueDate?: string;
    /**
     * Used only for TRL
     */
    dateReceived?: string;
    /**
     * Used only for TRL
     */
    datePending?: string;
    /**
     * Used only for TRL
     */
    dateAuthorised?: string;
    /**
     * Used only for TRL
     */
    dateRejected?: string;
}