TODO:

DONE: pull template into a separate file (json)
- STARTED: service to take the test result from the API and return the read-only template
DONE: move dynamic form rendering from Home page to test-results page - replacing the current page content
- STARTED: make the dynamic form output readonly and match the current markup/content
DONE: template to match the test result (existing 4 fields, plus the 5 new ones from the story)
- ANDREW: data model and mock-api data to match new 5 fields
DONE: map the API result onto the template (show the data from the API call) - dynamic forms example
DONE: add the defaultIfNullOrEmpty pipe to the output values  {{ value | defaultIfNullOrEmpty }}
- remove the old markup
- fix routing
- tidy-up
- test coverage
DONE: NIALL & TOM: help Tom with his Error message tests
- win
- profit
